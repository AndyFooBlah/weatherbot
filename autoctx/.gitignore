# Generated by /autoctx-evaluate and /autoctx-hillclimb runs.
# Eval reports, gap analyses, improved-context JSONs — keep the
# directory tracked (.keep) but not its contents.
experiments/*
!experiments/.keep
