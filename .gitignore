# personal config (contains MAC + paths; safe-ish but keep out of git by default)
infra/env.sh

# secrets — should never be in repo regardless
*.key
*.pem
service-account-*.json
.env
.env.*
!.env.example

# python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# build
dist/
build/

# downloaded binaries
agent/bin/

# ADK local session state (chat history sqlite)
**/.adk/

# editor / OS
.vscode/
.idea/
.DS_Store
scripts/.gda-grpc-venv/

# Exported Claude Code session transcripts — generated by
# scripts/export-transcript.py. Unredacted by default; contains the
# raw conversation including personal identifiers. Keep out of git.
docs/project-summary-log/transcript.md
docs/project-summary-log/transcript-*.md
